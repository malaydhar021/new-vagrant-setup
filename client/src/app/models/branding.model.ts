/**
 * BrandingModel is holding the data param for each request like 
 * `create`, `list`, `update` and `delete` brands.
 * @class BrandingModel
 * @version 1.0.0
 * @author Tier5 LLC `<work@tier5.us>`
 * @license Proprietary
 */
export class BrandingModel {
  name?: string;
  url?: string;
  branding_id?: number;
  id?: number;
}